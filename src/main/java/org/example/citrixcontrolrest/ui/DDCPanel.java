package org.example.citrixcontrolrest.ui;

import javax.swing.*;
import java.awt.*;

public class DDCPanel extends JPanel {
    public DDCPanel() {
        setLayout(new BorderLayout());
        add(new JLabel("Contenido DDCs", SwingConstants.CENTER), BorderLayout.CENTER);
        // Aquí más componentes y lógica específica UI DDCs
    }
}
